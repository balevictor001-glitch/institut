# Seed d'images locales

Ce dossier contient les images d'exemple utilisées pour pré-remplir les produits.

Le script `update_images.py` utilise ces fichiers locaux par défaut pour charger les images dans `media/produits/`.

Si une image locale est manquante, le script génère automatiquement une image de remplacement simple.
